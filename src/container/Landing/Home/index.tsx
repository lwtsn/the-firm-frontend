import React from 'react';
import { Wrapper } from './styled';

const SchemeWrapper: React.FC = () => {
    return (
        <Wrapper>
            <p>Scheme in progress:</p>
            <p>Training in progress:</p>
        </Wrapper>
    );
};

export default SchemeWrapper;
